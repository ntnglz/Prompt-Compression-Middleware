> **Legacy (ES)** — Versión en español pre-migración. Ver [getting-started en inglés](../getting-started.md).

# Inicio rápido (archivo legacy)

La guía actualizada está en inglés: [docs/getting-started.md](../getting-started.md).

Resumen:

1. `pip install -e ".[dev]"`
2. `python run.py --demo-stub` (sin Ollama)
3. Docker: `cp .env.example .env` → `docker compose up --build`
4. Proxy: `http://localhost:8090/v1/chat/completions`
