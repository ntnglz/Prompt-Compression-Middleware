# PCM core — compression engine (no HTTP/MCP)
ollama>=0.3.0
tiktoken>=0.5.0
pydantic>=2.5.0
numpy>=1.24.0
python-dotenv>=1.0.0

# Proxy mode (Docker default)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0
mistralai>=1.0.0

# MCP server
mcp>=1.0.0
